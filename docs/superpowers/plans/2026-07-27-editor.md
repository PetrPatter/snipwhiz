# Snipwhiz Editor Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A capture opens in an editor. Objects can be drawn, selected, restyled, moved, resized, rotated, undone and redone. Closing saves without asking. The library tile, preview and clipboard all show the annotated result. Re-opening restores every object still editable.

**Architecture:** Pure WPF. The scene graph is one `DrawingVisual` per annotation inside a `VisualCollection` host — no custom panel, no element-per-object. `Annotation.Render(DrawingContext)` lives in `Snipwhiz.Core` and is called by *both* the on-screen canvas and the flattener, which is what makes WYSIWYG a property rather than a hope. Geometry is stored in image-pixel space; a single `ScaleTransform` handles zoom. Undo holds command deltas, never bitmaps.

**Tech Stack:** .NET 10 · WPF (Core **and** App) + WinForms (`NotifyIcon`) · `Microsoft.Windows.CsWin32` · `Microsoft.Data.Sqlite` · `System.Drawing.Common` · xUnit

**Spec:** `docs/superpowers/specs/2026-07-27-editor-design.md` — read §1, §4.1, §4.13 and §7 before starting.

## Scope of this document

Spec 2b delivers in six phases (spec §8). **Phase A is specified here task-by-task.** Phases B–F are listed at task-title granularity at the end and get expanded when reached — later phases depend on what Phase A actually reveals, and writing forty speculative task bodies now would be fiction with checkboxes.

## Global Constraints

Every task's requirements implicitly include this section.

- **Spec 1's and spec 2a's Global Constraints still apply**, with one exception below. Platform floor Windows 11 22H2 / `net10.0-windows10.0.22621.0`; product name `Snipwhiz`; data path `%LOCALAPPDATA%\Snipwhiz\`; balloons not toasts; `TreatWarningsAsErrors`.
- **REVERSED: `Snipwhiz.Core` now references WPF.** Spec 2a's plan said "Core has no WPF reference and does not gain one." Spec 2b §1 supersedes that deliberately — `Render` must be shared by canvas and flattener. Core gains `UseWPF`; Core tests gain an STA runner. **This is the only reversal; every other constraint stands.**
- **Zero new third-party dependencies.** Everything is BCL, WPF, `System.Drawing.Common` or `Microsoft.Data.Sqlite`. If a task appears to need a NuGet package, stop and report rather than adding one.
- **SQLite is touched only from the UI thread.** Unchanged from 2a. The save pipeline flattens on a background STA thread and marshals back for the DB write.
- **Saved PNGs are never modified or rewritten.** The capture is immutable forever. Edits produce `projects/` and `flat/` files; deletion is still the only operation that touches an original.
- **There is exactly one copy-to-clipboard path**, through `ClipboardWriter.Write`. Never `System.Windows.Clipboard.SetImage`.
- **Every consumer resolves a capture through `CaptureAssets`** (spec §4.1). No new call site may reach for `store.ResolvePath` to decide what to *show*. Only the editor's source bitmap uses the original.
- **There is no dirty state.** Do not add an `IsDirty` flag, a save prompt, or a "discard changes" path. If a task seems to need one, the design has drifted — stop and report.
- **A guard must be observed failing before it is trusted.** Where a task names a negative control, run it, watch it fail, and put the failing output in the report. A check that has only ever passed is not evidence.
- **Automated gates must move the way a user moves.** Spec 2a's sweep scrolled one direction and never resized, and passed two UI regressions cleanly. Any new gate states, in its own report, what class of bug it is blind to.

## File Structure

```
src/
  Snipwhiz.Core/
    Snipwhiz.Core.csproj             EDIT UseWPF
    Annotations/
      Annotation.cs                  NEW  abstract base: transform, style, bounds, hit-test, render
      RectangleAnnotation.cs         NEW  Phase A's proving tool
      Style.cs                       NEW  one record, Color values not Brushes
      Transform2D.cs                 NEW  compose, invert, apply — unit-tested standalone
    Scene/
      SceneDocument.cs               NEW  annotations, crop, cutouts, z-order
      Commands/*.cs                  NEW  Add, Remove, Transform, RestyleZOrder
      UndoStack.cs                   NEW  bounded at 50, coalescing
    Project/
      ProjectStore.cs                NEW  .ssproj read/write, atomic
      UnknownAnnotation.cs           NEW  JsonElement passthrough for forward compat
    Imaging/
      Flattener.cs                   NEW  scene -> PNG via RenderTargetBitmap
      ThumbnailCache.cs              EDIT generate from CaptureAssets.Display
    Storage/
      CaptureAssets.cs               NEW  Display / Original / Project / All
      LibraryDb.cs                   EDIT schema v3, SetEditPaths
      CaptureStore.cs                EDIT pass-throughs

  Snipwhiz.App/
    App.xaml.cs                      EDIT editor wiring, EditSaved, hide-for-capture generalised
    Editor/
      EditorWindow.xaml / .cs        NEW  chrome, tool rail, zoom/pan, autosave-on-hide
      CanvasHost.cs                  NEW  VisualCollection scene graph
      SelectionAdorner.cs            NEW  handles, rotate, marquee
      Tools/ITool.cs                 NEW
      Tools/RectangleTool.cs         NEW
      Tools/SelectTool.cs            NEW
      SavePipeline.cs                NEW  project + flatten + DB + invalidate + notify
    Library/
      ClipboardCopier.cs             EDIT route through CaptureAssets.Display
      PreviewView.xaml.cs            EDIT route through CaptureAssets.Display, Edit button
      CaptureTileViewModel.cs        EDIT Invalidate(), IgnoreImageCache
      LibraryWindow.xaml.cs          EDIT EditSaved handling, delete over the full file set
    Diagnostics/
      WysiwygVerification.cs         NEW  canvas vs flattener pixel diff
      EditorMemoryVerification.cs    NEW  counted gate

tests/
  Snipwhiz.Core.Tests/
    StaFact.cs                       NEW  STA test attribute
    Annotations/TransformTests.cs    NEW
    Annotations/HitTestTests.cs      NEW
    Scene/UndoCommandTests.cs        NEW
    Project/ProjectStoreTests.cs     NEW  round-trip, golden file, unknown-type control
    Imaging/FlattenerTests.cs        NEW
    Storage/CaptureAssetsTests.cs    NEW
    Storage/MigrationTests.cs        EDIT v2 -> v3 sibling
    Fixtures/golden-v1.ssproj        NEW  committed, must keep loading
    Fixtures/unknown-type.ssproj     NEW  forward-compat negative control
```

**Task order is dependency order with risk pulled forward.** Tasks 1–7 are headless Core work, fully testable without a window. Task 8 produces the first pixels. Tasks 11–13 are the library integration the spec review flagged as the highest-risk area, and they land on a proven Core with named negative controls.

---

## Phase A — Foundation

### Task 1: Core gains WPF, and an STA test runner

Spec §1. First because every later Core task needs `DrawingContext`, and because if this is awkward it is better to know on day one than at task 6.

**Files:**
- Modify: `src/Snipwhiz.Core/Snipwhiz.Core.csproj`
- Create: `tests/Snipwhiz.Core.Tests/StaFact.cs`

**Interfaces:**
- Produces: Core able to reference `System.Windows.Media`; `[StaFact]` for tests needing the WPF render stack.

- [x] **Step 1: Add `UseWPF`**

Add `<UseWPF>true</UseWPF>` alongside the existing `<UseWindowsForms>true</UseWindowsForms>`. Both can coexist; WinForms is there for `NotifyIcon` and stays.

**Found:** `UseWPF` **removes `System.IO` from the implicit usings**, because `System.Windows.Shapes.Path` collides with `System.IO.Path`. 43 compile errors across `CaptureStore`, `Settings` and `ThumbnailCache`. The merged WinForms+WPF implicit set this project actually gets does *not* include `System.Windows.Shapes`, so nothing here collides and `<Using Include="System.IO" />` restores the SDK default — one line instead of a `using` in every file that touches a path. If a future file does pull in `System.Windows.Shapes`, `Path` becomes ambiguous and that surfaces as a compile error, not a silent change.

- [x] **Step 2: Write `StaFact`** → **delivered as `Sta.Run`, not an attribute**

`RenderTargetBitmap` and `DrawingVisual` require an STA thread. `ClipboardFormatTests` already spins one by hand — generalise it so tasks 6 and 15 do not each reinvent it.

**Deviation:** a real `[StaFact]` attribute needs either the `Xunit.StaFact` package — forbidden by Global Constraints — or a custom xUnit test framework, a few hundred lines to save one call per test. Delivered as `tests/Snipwhiz.Core.Tests/Sta.cs`, a static helper. `ClipboardFormatTests` now uses it.

One behaviour change while generalising: the original ignored `Thread.Join`'s return value, so a deadlocked STA thread returned the default result and the test **passed**. `Sta.Run` throws on timeout.

- [x] **Step 3: Prove it**

`WpfInCoreTests` builds a `DrawingVisual`, draws a filled red rect, renders to a 4×4 `RenderTargetBitmap` and asserts the pixel. An empty render leaves the buffer zeroed, so the red channel distinguishes "drew" from "did nothing".

**Verification — done.** Build clean, 0 warnings with `TreatWarningsAsErrors` on. 105 tests pass (104 existing + 1 new). CsWin32 generation unaffected. App launches and stays up.

**Outstanding for the user:** confirm the tray icon, its menu, and the capture hotkeys still work at runtime — a build cannot show that, and `UseWPF` changes what the process loads at startup.

---

### Task 2: Schema v3

Spec §5. Headless, and a prerequisite for the save pipeline.

**Files:**
- Modify: `src/Snipwhiz.Core/Storage/LibraryDb.cs`, `CaptureStore.cs`
- Test: `tests/Snipwhiz.Core.Tests/Storage/MigrationTests.cs`

**Interfaces:**
- Produces: `CaptureRecord` with `ProjectPath`, `FlatPath`, `FlatWidth`, `FlatHeight`, `EditedUtc`; `LibraryDb.SetEditPaths(...)`; `SchemaVersion == 3`.

- [x] **Step 1: Write the failing v2→v3 test first**

Build a **real v2 database by hand** — the v2 `CREATE TABLE`, the `idx_captures_created` index, `PRAGMA user_version = 2`, two rows — then open it with `LibraryDb`. Assert version 3, five new columns present and NULL, both rows readable, index intact.

**Observed failing** before the migration existed. The v1 test now covers v1→v3 in one open, which is the path for a library untouched since spec 1.

- [x] **Step 2: Add the v3 step**

- [x] **Step 3: Grow `CaptureRecord`, `SelectColumns`, `ReadAll`, `Insert`**

New columns are optional record parameters, so no existing construction site changed.

- [x] **Step 4: `SetEditPaths`**

**Verification — done.** 110 tests pass. Four new: `SetEditPaths` round-trip; a failed flatten leaving the project recorded and `flat_path` null; delete→undo keeping all five columns (the silent-severance case); and editing not moving a capture in the library.

**Out-of-task fix, flagged.** `ThumbnailCacheTests.Concurrent_requests_for_the_same_capture_all_succeed` was failing **4 runs in 6** — a live race shipped in spec 2a, not caused by this task. Two tiles requesting the same thumbnail both generate, then both `MoveFileEx` onto the same destination and the loser gets access-denied; in the app that is a tile that never renders. `ThumbnailCache`'s own comment claimed the duplicate work was "wasted but harmless" — nothing implemented the harmless part.

Fixed in `Publish`: if the destination is usable the other thread won, so drop the temp and succeed. A single probe was not enough — `IsUsable` opens the file, so a probe taken while the winner's replace is in flight hits a sharing violation and reports unusable — so it retries up to five times at 20 ms.

**0 failures in 40 consecutive runs**, against 4-in-6 before. That is strong evidence, not proof: it is a timing race and the sampling is statistical.

---

### Task 3: `CaptureAssets` — the single display path

Spec §4.1. **This is the fix for the review's highest-severity finding** and it lands before anything can write a flat render, so no consumer is ever written against the old assumption.

**Files:**
- Create: `src/Snipwhiz.Core/Storage/CaptureAssets.cs`
- Modify: `ThumbnailCache.cs`, `src/Snipwhiz.App/Library/ClipboardCopier.cs`, `PreviewView.xaml.cs`, `LibraryWindow.xaml.cs`
- Test: `tests/Snipwhiz.Core.Tests/Storage/CaptureAssetsTests.cs`

**The existing problem.** Three call sites independently resolve a capture, and all three take the original: `ThumbnailCache.Generate`, `ClipboardCopier.cs:27`, `PreviewView.xaml.cs:74`. Redirecting only the thumbnail — which is what rev. 1 of the spec did — ships a grid showing the annotated image while preview and clipboard hand back the un-annotated one.

- [x] **Step 1: Write `CaptureAssets`**

`Display` uses `record.FlatPath` as the authority on whether a render was ever saved — no second source of truth, and no directory probing for unedited captures, which is the overwhelming majority. The existence check applies only to captures claiming a render, so a deleted or half-written flat file degrades to the original.

`All` deliberately resolves project and flat from the **id**, not the columns: a save that crashed between writing `flat/<id>.png` and recording it leaves a file no row references, and resolving from columns would orphan it forever.

Also removed a duplicate of the thumbs layout from `ThumbnailCache`, which had its own `ThumbsDir`.

- [x] **Step 2: Route the consumers through `Display`**

Three, not five: **Reveal-in-Explorer and Open-with-default-viewer do not exist.** Spec 2a §2 listed them as in-scope but there is no `Process.Start` anywhere in `Snipwhiz.App`. Noted rather than built — adding them is not this task.

- [x] **Step 3: Tests**

Six, none of which need a database. Includes one asserting an unedited capture resolves **without touching the disk**, and one asserting `All` names an orphaned render the columns never recorded.

**Verification — done, both directions.** `SNIPWHIZ_SEED_EDITED=n` was added to `LibrarySeed` to fake an editor save on the newest n captures, writing a garish magenta render and recording it with `SetEditPaths`. There is no editor yet, and this is the only way to check the routing before one exists to blame.

| Scenario | Result |
|---|---|
| Renders present | exactly 3 of 12 thumbnails magenta — the edited ones |
| Renders deleted, rows still claim them | 0 magenta, **0 broken** — clean fallback |

Thumbnail results measured on disk by sampling centre pixels. Preview and clipboard confirmed by eye against the same seeded library: magenta tile → magenta preview → magenta paste into Paint.

---

### Task 4: Transforms and hit-testing, unit-tested before any UI

Spec §4.4, §4.6, risk 4. Rotated hit-testing is hand-rolled and easy to get subtly wrong; the maths gets proven against known points while it is still pure functions.

**Files:**
- Create: `src/Snipwhiz.Core/Annotations/Transform2D.cs`, `Style.cs`, `Annotation.cs`, `RectangleAnnotation.cs`
- Test: `tests/Snipwhiz.Core.Tests/Annotations/TransformTests.cs`, `HitTestTests.cs`

- [x] **Step 1: ~~`Transform2D`~~ — dropped, WPF's `Matrix` already is one**

`System.Windows.Media.Matrix` is an affine 2D transform with `Invert`, `Multiply` and `Transform(Point)`, and Core references WPF as of task 1. Writing our own would re-implement tested platform code, and it serialises as the six numbers spec §4.11 already specifies.

This also sharpened the tests: Microsoft's matrix maths is not what can be wrong — *our use of it* is. So the tests moved onto `Annotation.HitTest`.

- [x] **Step 2: `AnnotationStyle` as a record of `Color` and numbers**

Named `AnnotationStyle`, not `Style`: `System.Windows.Style` is imported by every WPF file, and shadowing it would be a permanent papercut across the ~15 annotation types still to come.

- [x] **Step 3: `Annotation` base and `RectangleAnnotation`**

Two invariants worth knowing, both documented at their definitions:

- **Transform carries rotation and translation only, never scale.** Resizing edits the object's geometry, which keeps a stroke the width the user chose, and keeps the transform a rigid motion so a hit tolerance means the same distance in both spaces.
- **Local geometry is centred on the origin**, so rotation turns an object about its own middle with no correction term.

- [x] **Step 4: Hit-test tests, rotated**

**Verification — done, and it caught a flaw in the test itself.** Both wrong implementations were built and run:

| Negative control | Caught by the headline test? |
|---|---|
| Test `Bounds` instead of the inverse transform | yes |
| Apply the transform instead of its inverse | **no — originally missed** |

The first version used a square at 45°, where rotating the probe the wrong way lands it symmetrically outside, so it missed the inverse bug entirely while looking like it discriminated. Replaced with a 200×60 rectangle at 30° and a probe at `R(-30°)·(95,0)`, which separates all three outcomes: correct misses, forgot-to-invert hits, bounds-check hits. Both controls now fail it.

124 tests pass.

---

### Task 5: `SceneDocument` and `.ssproj`

Spec §4.11.

**Files:**
- Create: `src/Snipwhiz.Core/Scene/SceneDocument.cs`, `Project/ProjectStore.cs`, `Project/UnknownAnnotation.cs`
- Test: `tests/Snipwhiz.Core.Tests/Project/ProjectStoreTests.cs`, fixtures

- [x] **Step 1: `SceneDocument`** — capture id, schema, ordered annotations, crop. Cutouts, border and edges are phase D and are absent rather than stubbed; JSON tolerates their arrival.

- [x] **Step 2: `ProjectStore` write, atomic** — unique temp then move.

- [x] **Step 3: Unknown-type passthrough**

Written as one hand-rolled converter rather than `[JsonPolymorphic]`. The attribute covers the easy part and then throws on an unknown discriminator — exactly the case that must not throw — and intercepting that needs a custom converter anyway, at which point the attribute has bought nothing. Doing it explicitly also keeps the whole wire format legible in one file.

- [x] **Step 4: Golden file and negative controls**

**Verification — done, both controls seen failing.**

| Control | Result |
|---|---|
| Unknown types rejected instead of preserved | 2 tests fail |
| Loader that returns an empty scene | **5 tests fail, including the golden one** |

The second is the one that mattered: the golden test asserts ids, sizes, transform components and style colours, so it catches a loader that silently returns nothing. A bare no-throw assertion would have passed it.

**Found: `.ssproj` fixtures were silently absent from the test output.** MSBuild's `DefaultItemExcludes` contains `**/*.*proj`, and our extension ends in `proj`, so the files were never in the default `None` set and there was nothing for `None Update` to modify. It fails as missing files at runtime, not as a build error. Fixed with `None Include`. Worth remembering if `.ssproj` ever needs globbing elsewhere.

135 tests pass.

---

### Task 6: The flattener

Spec §4.12. Needs `[StaFact]` from task 1.

**Files:**
- Create: `src/Snipwhiz.Core/Imaging/Flattener.cs`
- Test: `tests/Snipwhiz.Core.Tests/Imaging/FlattenerTests.cs`

- [x] **Step 1: Render a `SceneDocument` over a source bitmap to a `RenderTargetBitmap` at image scale, encode PNG, write atomically.**

Uses WPF's `PngBitmapEncoder`, **not** the project's own `PngEncoder`. `RenderTargetBitmap` only produces `Pbgra32` — premultiplied — and `PngEncoder` takes straight BGRA. Today the canvas is opaque throughout so the two agree, which is exactly what makes it a trap: it would start being wrong the first time an annotation had partial alpha at the image edge.

Crop is clamped to the capture and never empty. Both a crop reaching outside the source and a zero-width one are reachable from a hand-edited project, and the second throws inside `RenderTargetBitmap`.

- [x] **Step 2: Tests against known scenes** — 11 of them, covering placement, z-order vs list order, rotation agreeing with hit-testing, crop offset and clamping, and save round-tripping through a real PNG.

**Verification — done, both controls seen failing, and the second exposed a weak test.**

| Control | Result |
|---|---|
| Annotations never rendered | 6 tests fail |
| Crop offset dropped | 1 test fail → **2 after strengthening** |

`An_annotation_outside_the_crop_does_not_appear` originally sampled pixels where the un-offset rectangle would not have landed anyway, so it passed against the bug it was named for. It now samples `(10,10)` — exactly where the annotation appears if image coordinates are read as output coordinates.

146 tests pass.

---

### Task 7: Undo stack and commands

Spec §4.7.

**Files:**
- Create: `src/Snipwhiz.Core/Scene/UndoStack.cs`, `Commands/*.cs`
- Test: `tests/Snipwhiz.Core.Tests/Scene/UndoCommandTests.cs`

- [x] **Step 1: `ISceneCommand` with `Do`/`Undo` holding deltas only** — no bitmaps, ever.

- [x] **Step 2: Six commands, not the five listed**

`Add`, `Remove`, `Move`, **`Reshape`**, `Restyle`, `Reorder`. Reshape is a consequence of task 4: because `Transform` carries no scale, resizing edits the object's own geometry, and undo therefore has to capture geometry. Handled with `CaptureGeometry`/`RestoreGeometry` on `Annotation` so it stays one command rather than one per shape across the fifteen types to come.

- [x] **Step 3: `UndoStack`** bounded at 50 by count, with absorbing.

Absorbing lets a tool apply each step of a drag live — the scene is correct at every frame — while the stack still records one entry. The alternative is every tool buffering its own start state and applying once on release, which is the same logic written once per tool.

- [x] **Step 4: Do/undo/redo identity test per command type**

The harness compares scenes **through the real serializer**, so the check cannot drift from what a save produces, and it covers fields a future annotation type adds without touching the test.

**Verification — done. The harness's own control is now a permanent test.**

| Control | Result |
|---|---|
| A command whose `Undo` is a no-op | harness throws — asserted by `The_round_trip_harness_catches_a_command_that_does_not_undo` |
| Absorbing removed | 3 tests fail |
| Redo branch not discarded on a new action | 1 test fails |

The first is kept as a test rather than run once by hand: a harness that has only ever seen working commands is not known to catch a broken one, and that stays true next month.

161 tests pass. **Phase A's headless Core work is complete.**

---

### Task 8: `CanvasHost` — the scene graph, and the first pixels

Spec §4.2, §4.3.

**Files:**
- Create: `src/Snipwhiz.App/Editor/CanvasHost.cs`

- [ ] **Step 1: `FrameworkElement` overriding `GetVisualChild` / `VisualChildrenCount` over a `VisualCollection`.** One `DrawingVisual` per annotation.

- [ ] **Step 2: Per-object invalidation** — changing one annotation re-renders that visual only, not the scene.

- [ ] **Step 3: Zoom and pan** — a single `ScaleTransform` plus `TranslateTransform`; 10%–800%, `Ctrl`+wheel around the cursor, space-drag and middle-drag to pan, fit-on-open above viewport size and 100% otherwise (spec §4.18).

- [ ] **Step 4: Screen ↔ image coordinate conversion**, with handle sizes and hit tolerances dividing by zoom (spec §4.3).

**Verification:** A temporary debug command that loads a scene with 200 rectangles and reports render time on zoom change. Confirm that moving one rectangle re-renders one visual — instrument a counter in the render override, do not assume. Report the number.

---

### Task 9: Selection, handles and rotate

Spec §4.6.

**Files:**
- Create: `src/Snipwhiz.App/Editor/SelectionAdorner.cs`, `Tools/SelectTool.cs`

- [ ] **Step 1: Eight resize handles, one rotate handle, a move region.** Handle hit-testing runs before object hit-testing, in screen space, at fixed tolerance.
- [ ] **Step 2: Handle hit-testing through the inverse transform** for rotated objects, using task 4's tested maths.
- [ ] **Step 3: Marquee and Ctrl-click multi-select**, union bounds, group transform composed onto members on commit.
- [ ] **Step 4: Shift constrains aspect / 15° rotation; Alt resizes about centre.**

**Verification:** Manual, and enumerated with expected outcomes: rotate a rectangle 45°, then grab each of the eight handles and confirm each resizes along the *rotated* axis, not the screen axis. Confirm a click inside the bounding box but outside the rotated shape deselects rather than drags.

---

### Task 10: `EditorWindow`, the rectangle tool, and autosave

Spec §4.14, §4.18. First runnable editor.

**Files:**
- Create: `src/Snipwhiz.App/Editor/EditorWindow.xaml` / `.cs`, `Tools/ITool.cs`, `Tools/RectangleTool.cs`

- [ ] **Step 1: Window shell** — Mica, dark theme matching the library, thin left tool rail with two entries (select, rectangle).
- [ ] **Step 2: `RectangleTool`** — create by drag, producing an `AddAnnotation` command. Tools never mutate the scene directly.
- [ ] **Step 3: Escape unwinds innermost-first** — active drag → selection → hide the window. Each level consumes the key only if active.
- [ ] **Step 4: Shortcuts** — `Ctrl+Z`/`Y`, `Ctrl+S`, `Ctrl+D`, `Delete`, arrow nudge 1 px / `Shift` 10 px, `Ctrl+0` fit, `Ctrl+1` 100%, `[`/`]` z-order.
- [ ] **Step 5: Hide rather than close, autosaving on hide.** No dirty flag anywhere.

**Verification:** Draw, select, resize, rotate, undo, redo, hide, reopen — the rectangle is still there and still editable.

---

### Task 11: The save pipeline

Spec §4.12.

**Files:**
- Create: `src/Snipwhiz.App/Editor/SavePipeline.cs`
- Modify: `App.xaml.cs`

- [ ] **Step 1: Write `.ssproj`, then flatten, on a dedicated STA background thread.** A 4K `RenderTargetBitmap` on the UI thread is a visible hitch, and autosave-on-close makes it happen at the worst moment.
- [ ] **Step 2: Marshal back for the DB update** — `SetEditPaths` — because SQLite stays on the UI thread.
- [ ] **Step 3: A failed flatten still commits the project.** `Display` falls back to the original; annotations are never lost because a render failed.
- [ ] **Step 4: Raise `EditSaved(record)`**, the sibling of the existing `CaptureCompleted`.

**Verification:** Make the flattener throw on demand; confirm the `.ssproj` is written, the library falls back to the original, and reopening the editor restores the objects. This is the failure path that decides whether a render bug costs someone their work.

---

### Task 12: Making the library actually refresh

Spec §4.13. **The review's second-highest finding, and the bug the spec itself predicted.**

**Files:**
- Modify: `CaptureTileViewModel.cs`, `LibraryViewModel.cs`, `LibraryWindow.xaml.cs`

**The existing problem, verified in source.** Four mechanisms each independently prevent a refresh: `LibraryViewModel` keeps one view model per capture id forever (`LibraryViewModel.cs:17`); `CaptureTileViewModel` latches `_loaded` and returns early on later binds (`:86`); `InsertNewest` dedupes by id so re-raising the capture event is a no-op; and the decode uses `UriSource` without `IgnoreImageCache` (`:100`), so WPF's global cache can serve a stale bitmap for a path whose contents changed.

- [ ] **Step 1: `CaptureTileViewModel.Invalidate()`** — clear `Thumbnail` and `_loaded`, re-request **only if still bound**, reusing the binding count added when thumbnail retention was fixed rather than inventing a second liveness notion.
- [ ] **Step 2: Decode with `BitmapCreateOptions.IgnoreImageCache`.**
- [ ] **Step 3: `LibraryWindow` handles `EditSaved`** by calling `ThumbnailCache.Remove(id)` then `Invalidate()`.

**Verification — negative control required.** Build with the `EditSaved` handler suppressed; edit a capture and save; confirm the tile keeps showing the un-annotated thumbnail. Restore the handler and confirm it updates. Then, separately, keep the handler but remove `IgnoreImageCache` and confirm the stale bitmap comes back — the two failure modes are independent and each must be seen.

---

### Task 13: Entry point, delete over the full file set, and hide-for-capture

Spec §4.14, §4.15, §4.17.

**Files:**
- Modify: `PreviewView.xaml` / `.cs`, `LibraryWindow.xaml.cs`, `App.xaml.cs`

- [ ] **Step 1: An Edit button in the preview**, beside Copy — the slot spec 2a §6.4 reserved. A second open request autosaves the current document and switches.
- [ ] **Step 2: `CommitDelete` deletes over `CaptureAssets.All`** — original, thumbnail, flat, project. Currently it deletes two of four (`LibraryWindow.xaml.cs:165`), so projects and flat renders would be orphaned forever.
- [ ] **Step 3: `DropFileReferenceFromClipboard` matches against `All`**, not just the original (`:219`). Otherwise copy-then-delete of an edited capture pastes a dead file reference — spec 2a's check 11, reintroduced through a new path.
- [ ] **Step 4: Deleting the capture currently open** closes the editor after its autosave.
- [ ] **Step 5: Generalise hide-for-capture to all app windows.** `HideLibraryForCapture` knows only `_library` (`App.xaml.cs:149`). Restore on **every** abort path spec 1 §4.11 enumerates: Esc, right-click, watchdog, display change, focus refusal.

**Verification:** Copy an edited capture, delete it, paste — must not offer a dead file reference (this is check 11's exact shape on a new path). With the editor open, press the capture hotkey and confirm the editor is not in the grab; then abort the capture five ways and confirm the editor comes back each time.

---

### Task 14: The WYSIWYG gate

Spec §7. Without this, "export doesn't match what I drew" is a whole bug class with no check.

**Files:**
- Create: `src/Snipwhiz.App/Diagnostics/WysiwygVerification.cs`

- [ ] **Step 1: Render a known scene through the canvas and through the flattener; pixel-diff the two.**
- [ ] **Step 2: Report max channel delta and differing-pixel count**, not a boolean.

**Verification — negative control:** an env-gated build where the flattener applies a 1 px offset. The diff must be non-zero and the report must say where. Then confirm the unmodified path diffs to zero.

---

### Task 15: The editor memory gate

Spec §4.19. The library reached 708 MB retaining 320 px thumbnails; the editor holds 33 MB bitmaps.

**Files:**
- Create: `src/Snipwhiz.App/Diagnostics/EditorMemoryVerification.cs`

- [ ] **Step 1: Count live decoded source bitmaps and blur caches**, in the shape of `retainedThumbnails` — a counter, not a working-set eyeball. The managed heap will read small even when bitmaps are retained, because WPF keeps decoded pixels in unmanaged WIC memory.
- [ ] **Step 2: Open, edit, close, repeat ten times; report the counter and the working set.**

**Verification — negative control:** an env-gated build that retains the source bitmap after close. The counter must climb to 10. Without that, this gate is exactly the check that "has only ever passed."

---

## Phases B–F — task titles only

Expanded when reached.

**Phase B — Core tools + minimum style.** Arrow · ellipse · line · text (with the `TextBox` overlay and its pixel-diff metric check) · highlight · crop with dimmed draggable region · a colour and width control · per-tool default persistence with atomic `Settings` writes.

**Phase C — Redaction and emphasis.** Blur with box-during-drag and Gaussian-on-release · pixelate · spotlight · magnify · step numbers · callout with tail.

**Phase D — Remaining tools.** Pen · polygon · stamps and the starter-set decision · cut-out with band mapping and its property test · canvas resize · border · edge effects · flat-dimension consumers.

**Phase E — Full style system.** Contextual floating toolbar with flip-on-overflow · multi-select restyle across mixed types · shadow presets · fills · arrowhead shapes.

**Phase F — Re-host.** `LibraryView` control · one shared `LibraryViewModel` · pull-up sheet with spring easing · the orphaned `LibraryWindow` behaviour spec §4.16 enumerates (Esc routing, wheel override, Mica, hide-triggered `FlushPendingDeletes`, the static `RemoveRequested` double-subscribe) · the capture-to-editor setting.

---

## Verification summary

| Gate | Task | Negative control |
|---|---|---|
| Migration v2→v3 | 2 | Hand-built v2 DB; column round-trip through delete/undo |
| Display path | 3 | Delete the flat file, confirm fallback in all three consumers |
| Rotated hit-test | 4 | Transpose instead of invert; rotated-miss test must fail |
| Golden file | 5 | Bare no-throw assertion must pass on an empty-scene stub |
| Forward compat | 5 | Remove the passthrough converter; unknown-type test must throw |
| Flattener draws | 6 | Empty the annotation list; pixel assertion must fail |
| Undo identity | 7 | A command with a no-op `Undo` must be caught |
| Per-object render | 8 | Instrumented counter, reported |
| Tile refresh | 12 | Suppress `EditSaved`; then separately remove `IgnoreImageCache` |
| Dead file reference | 13 | Copy edited capture, delete, paste |
| WYSIWYG | 14 | 1 px flattener offset must produce a non-zero diff |
| Editor memory | 15 | Retain-after-close build; counter must reach 10 |

**What these gates are blind to**, stated up front per Global Constraints: none of them exercise DPI changes mid-session, monitor changes with the editor open, or captures larger than 4K. Those stay manual, and they are enumerated with expected outcomes in the verification doc when Phase A completes.
