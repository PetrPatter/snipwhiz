<UserControl x:Class="Snipwhiz.App.Library.PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed">

    <Grid Background="#F21C1B1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="4,4,4,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton" Grid.Column="0"
                    Content="←  Back"
                    Background="Transparent" BorderThickness="0"
                    Foreground="#F5F2EC" FontSize="13"
                    Padding="10,6" Cursor="Hand"/>

            <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText"
                           Foreground="#F5F2EC" FontSize="13"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="MetaText"
                           Foreground="#8F8A80" FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>

            <Button x:Name="CopyButton" Grid.Column="2"
                    Content="Copy"
                    Background="#E8833A" BorderThickness="0"
                    Foreground="#1C1B1A" FontSize="13" FontWeight="SemiBold"
                    Padding="18,7" Cursor="Hand"/>
        </Grid>

        <!-- Image surface. Clicking the empty space around the image dismisses. -->
        <Grid x:Name="ImageSurface" Grid.Row="1" Background="Transparent">
            <Image x:Name="FullImage"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Stretch="Fill"/>

            <TextBlock x:Name="StatusText"
                       Foreground="#8F8A80" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
